import { BlogPlantasDirective } from './blog-plantas.directive';

describe('BlogPlantasDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogPlantasDirective();
    expect(directive).toBeTruthy();
  });
});
