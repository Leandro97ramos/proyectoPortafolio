import { FormatearFechaPipePipe } from './formatear-fecha-pipe.pipe';

describe('FormatearFechaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearFechaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
